# Copiar para /etc/systemd/system/retreino.timer no host e habilitar com:
#   sudo systemctl enable --now retreino.timer
[Unit]
Description=Job diario de drift/retreino do modelo de churn

[Timer]
OnCalendar=*-*-* 06:00:00
Persistent=true

[Install]
WantedBy=timers.target
